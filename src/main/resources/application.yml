
spring:
#  config:
#    import: file:.env[.properties]
  redis:
    host: localhost  # Redis 서버 호스트
    port: 6379       # Redis 서버 포트 (기본값: 6379)
    password:
  profiles:
    active: dev # dev or prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      max-lifetime: 300000
      maximum-pool-size: 10
      idle-timeout: 30000
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
jwt:
  secret: ds3eg4sd4as3rr313434112r234dds3rr31343343rt3ws3rr313434112as3rr313434112fds6dfs
  expire:
    access: 3600 # access token 만료 시간을 초 단위로 설정
    refresh: 86400 # refresh token 만료 시간을 초 단위로 설정 (예: 하루)

server:
  port: 8080
  forward-headers-strategy: native
  servlet:
    session:
      timeout: 1440m
  max-http-header-size: 3145728

logging:
  level:
    root: warn
    org.mongodb.driver: warn
    org.springframework: info
    org.springframework.security: debug
    com.ico.api: debug

---

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://database-1.cz4eukq2qs6a.ap-northeast-2.rds.amazonaws.com:3306/moneyvill
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME_PROD}
    password: ${DB_PASSWORD_PROD}
  data:
    mongodb:
      uri: ${MONGODB_SRC_PROD}

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/stock
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: e3pooq^^
  data:
    mongodb:
      uri: mongodb+srv://sparta:jungle@cluster0.giqtz.mongodb.net/moneydb?retryWrites=true&w=majority&appName=Cluster0
